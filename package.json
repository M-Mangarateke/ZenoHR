{
  "name": "zenohr-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore security rules unit tests — TC-SEC-030",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"npx mocha tests/firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0",
    "mocha": "^10.4.0"
  }
}
